{
  "name": "web-proxy-site",
  "version": "1.0.0",
  "description": "A simple self-hosted web proxy (ProxySite-style) that fetches pages server-side and rewrites links so browsing continues through the proxy.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "express": "^4.21.0"
  }
}
